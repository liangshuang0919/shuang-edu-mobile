// 导入 vue 对象
import Vue from 'vue'
// 导入 vue-router 类
import VueRouter from 'vue-router'

// 挂载
Vue.use(VueRouter)

// 创建路由规则
const routes = []

// 实例化路由对象
const router = new VueRouter({
  routes
})

// 导出路由
export default router
